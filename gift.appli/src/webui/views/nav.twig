<nav>
    <ul>
        {% if user %}
            <li><span>connecté : {{ user.email }}</span></li>
        {% endif %}
        {% for item in globals.menu %}
            <li><a href="{{ url_for(item.route) }}">{{ item.text }}</a></li>
        {% endfor %}
        <li><a href="{{ url_for('home') }}" class="nav-link">home</a></li>
        {% if user %}
            <li><a href="{{ url_for('deco') }}" class="nav-link">se déconnecter</a></li>
        {% else %}
            <li><a href="{{ url_for('signin') }}" class="nav-link">se connecter</a></li>
        {% endif %}
        <li><a href="{{ url_for('box_post') }}" class="nav-link">fait ta box</a></li>
        <li><a href="{{ url_for('my_box') }}" class="nav-link">look ta box</a></li>
    </ul>
</nav>