{% extends "index.twig" %}

{% block content %}
    <h1>La Prestation {{ prestation.libelle }}</h1>

    <p>{{ prestation.description }}</p>
    <p>{{ prestation.tarif }}€</p>
    
    <br>
    <a href="{{ url_for('presta2', {}, {'cat_id': prestation.cat_id}) }}">Retour</a>
{% endblock %}