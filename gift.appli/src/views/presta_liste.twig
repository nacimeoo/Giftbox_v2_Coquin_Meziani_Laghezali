<h1>Prestations de la catégorie {{ categorie.libelle }}</h1>

<ul>
    {% for p in prestations %}
        <li>
            <a href="{{ url_for('prestation', {}, {'id': p.id}) }}"> <strong>{{ p.libelle }}</strong> - {{ p.tarif }}€</a>
        </li>    
    {% endfor %}
</ul>
